import { RedcolorelDirective } from './redcolorel.directive';

describe('RedcolorelDirective', () => {
  it('should create an instance', () => {
    const directive = new RedcolorelDirective();
    expect(directive).toBeTruthy();
  });
});
